# Base URL da API. Embutida no bundle NO MOMENTO DO BUILD (prefixo VITE_).
# Local: a API roda em :3000 (nesta máquina, :3001 — ver STATUS.md).
# Produção (Vercel): defina como a URL pública do serviço da Railway,
#   ex: https://fazmais-api.up.railway.app
VITE_API_BASE_URL="http://localhost:3000"
